upstream my_upstream {
    server localhost:{{new_app_port}};  # Replace 8082 with the correct port
}


server {
    listen {{nginx_listener}};
    location / {
        proxy_pass http://my_upstream;
    }
}
